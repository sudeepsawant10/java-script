const cart = ["shoes", "pants", "kurta"];

// pyramid of doom = many levels of nested indentation
// Invertion control = code gets out of control in a loop
// asynch exist because of callback exist
// cb are syncronous in nature 
/**
 api.createOrder(cart function(){
    api.proceedToPayment(function(){
        api.....
    });
 })
 */